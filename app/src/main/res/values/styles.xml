<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:progressBarStyle">@style/ProgressBarStyle</item>
        <item name="android:alertDialogTheme">@style/AlertDialogTheme</item>
        <item name="android:textColorLink">@color/primary_dark</item>
        <!--
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@transition/activity_explode</item>
        <item name="android:windowExitTransition">@transition/activity_explode</item>
        <item name="android:windowSharedElementEnterTransition">@transition/change_image_transform</item>
        <item name="android:windowSharedElementExitTransition">@transition/change_image_transform</item>-->
    </style>

    <style name="TextViewStatDisplay" parent="AppTheme">
        <item name="android:textSize">20sp</item>
    </style>
    
    <style name="ProgressBarStyle" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:theme">@style/Progress</item>
    </style>
    <style name="Progress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/accent</item>
        <item name="android:progressBackgroundTint">@color/accent_light</item>
    </style>

    <style name="AlertDialogTheme" parent="android:Theme.Material.Light.Dialog.Alert">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorAccent">@color/primary_dark</item>
    </style>


</resources>
